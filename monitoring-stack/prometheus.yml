global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets:
        - "localhost:9090"

  - job_name: "python-monitoring"
    static_configs:
      - targets: 
        - "monitoring-app:8000"
        